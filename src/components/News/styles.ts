import styled from "styled-components";

export const Container = styled.div`
  font-size: 14px;
  display: flex;
  flex-direction: column;

  > span {
    color: var(--gray);
    margin-bottom: 3px;
  }
`;
